(function (w, d) {
  var $ = function (x) {
        return document.getElementById(x);
      }
    , form = $("gen")
    , page {
        title: $("title").value,
        charset: $("charset").value
      };

  
}(window, window.document));